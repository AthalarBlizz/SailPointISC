# Module — Migration programs

> **Unit ID:** `m19`  
> **Path:** B · Implementation  
> **Source of truth for Listen mode.** Edit paragraphs below; blank lines separate spoken utterances.

---

Module nineteen is migration programs — yearly collections to per-service paths at org scale. Cold open: July two thousand twenty-six made versioning a program, not a pull request. Scripts, workflows, ServiceNow, R P A, and notebooks all hide debt.

Outcomes: build an inventory across those surfaces; apply official migration scripts and path tables while quarantining V two outliers; communicate Q two twenty twenty-eight support end and Q one twenty twenty-nine hard end of life.

Program shape, step one: inventory repos, workflow H T T P actions, ITSM integrations, R P A, and notebooks. Step two: classify each as already slash service slash v N, yearly, slash latest, or experimental. You cannot migrate what you have not found.

Automate what you can: software development kit migration scripts for TypeScript, Python, Go, and PowerShell, plus Workflow Analyzer for H T T P actions. Manual lane: V two outliers — some entitlements and access-request config paths are not blindly V one. Blind search-replace to slash v one will break them.

Prove with contract tests and dual-run where risk is high. Cut slash latest — it is not a production strategy because it can retarget silently under the new model. Govern with continuous integration deny-lists for slash v two zero two four, slash v two zero two five, slash v two zero two six, and slash latest in new code.

Leadership one-liner — say this: per-service versioning is current. Legacy yearly, v three, and beta remain until Q one twenty twenty-nine; support tickets through Q two twenty twenty-eight. We pin slash service slash v N and S D K two point x. Slash latest is not our production strategy.

Partial-done trap: app code migrated but workflows still on yearly paths; S D Ks upgraded but a slash latest cron remains; ITSM call sheets never updated. Declaring done early creates a surprise outage on the executive clock.

Single-file rewrites still use the official mapping table — skip project-management theater, not the table. Enterprise programs need inventory evidence, Workflow Analyzer output, a V two outlier register, and a published timeline.

Failure modes: migrating apps but not workflows; big-bang without inventory; done declared while slash latest survives; ignoring V two outliers until prod breaks.

When you’re back at the screen: outline a plan for v two zero two four scripts, v two zero two five workflows, and one slash latest job — inventory, map, migrate, analyze, pin, test, track to the two executive dates.
