# Module — Capstone portfolio

> **Unit ID:** `m20`  
> **Path:** B · Implementation  
> **Source of truth for Listen mode.** Edit paragraphs below; blank lines separate spoken utterances.

---

Module twenty is the capstone portfolio overview. Cold open: this is where Path B proves senior delivery — not by watching modules, but by shipping evidence across REST, software development kits, extensibility, and an architecture decision record.

Outcomes: complete or design Path A style capstones A through D on current per-service paths; deliver Path B capstones E through H covering transform, workflow, connector, and A D R; pass decision drills that force extension-point selection under constraints.

Portfolio map in spoken form. A, emergency disable — REST integration spec and lifecycle by name. B, compliance bridge — S D K automation with dry-run. C, peer provisioner — access requests with two point x naming. D, migration advisory — program timeline and tooling.

Extensibility lane: E is transforms JSON design. F is workflow H T T P actions on slash service slash v N. G is a SaaS Connectivity connector sketch. H is the architecture decision record that ties standards together. Decision drills sit beside these so you justify transform versus rule versus workflow versus connector.

Definition of done for senior work: current paths only — slash accounts slash v one style — and S D K methods with V one or V two suffixes; name-to-I D resolution; verify GETs; dry-run where mutating bulk; explicit when-not justifications; secrets story with no credentials in artifacts.

How to use the portfolio: treat labs as an evidence pack for promotion, interview, or team enablement. Cite modules four through nineteen inside each deliverable. Do not start here cold — foundations and A P I craft come first.

Say this when someone demos yearly paths as “the standard”: greenfield pins per-service versions; legacy is a migration clock to Q one twenty twenty-nine; slash latest is unsafe for production. Capstones that still center slash v two zero two six fail the bar.

Which labs prove what: extensibility is E, F, G, and the decision lab. A P I craft is A through C plus the REST and TypeScript implementation labs. Architecture standards live in H, fed by modules seventeen through nineteen.

Every mutator capstone must show: resolve by name, pin per-service path or S D K versioned methods, and verify with GET — plus dry-run for bulk. A beautiful A D R with no inventory or lab evidence is theater. A connector capstone that is actually a cron script is miscategorized work.

Failure modes: capstone still teaching yearly as current; A D R without evidence; connector that should have been a script; skipping verify and dry-run on mutators; credentials left in demo artifacts.

Enterprise checklist voice: all A through H briefs read; at least one REST, one S D K, and one extensibility artifact complete; decision scenarios answered with rationale; personal glossary of tenant-specific names you must resolve at runtime.

When you’re back at the screen: pick your next incomplete letter A through H, write the one-sentence proof it must demonstrate, and schedule the decision drill that forces an extension-point choice under a real constraint from your org.
