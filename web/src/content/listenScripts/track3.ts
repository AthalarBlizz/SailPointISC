/** Track 3 Listen scripts — Extensibility (m12–m16). Coach voice for Web Speech. */

export const moduleListenScriptsTrack3: Record<string, string[]> = {
  m12: [
    'Module twelve is transforms — attribute mapping without code. Cold open for Identity Security Cloud: if you jump to BeanShell to lowercase an email, you just bought review and install debt that a transform would have avoided. Start at the lightest extension point.',
    'Outcomes: author and read transform JSON shaped with type, attributes, and input; decide transform versus rule with a clear complexity boundary; reference transforms from identity profiles and resolve objects by name when you manage them through A P Is.',
    'A transform is deterministic calculate-and-map during aggregate or provision. Common patterns: concat, lower, upper, substring, date format, firstValid, and lookup. Use them to standardize email, user principal name, and display name across sources.',
    'Mental model of the extensibility ladder you will climb through this track: transforms first, then workflows for event process, then SaaS connectors when you need a Source, then customizers to tweak connector I O, then rules when the vocabulary truly fails, then external A P I jobs for heavy batch. Prefer the lightest fit.',
    'Shape without dumping code: a lower transform wraps an account attribute input from a named source — for example H R source email. A firstValid transform tries preferred name, then legal name, and takes the first non-empty value. Compose small transforms rather than one opaque monster.',
    'You can list and manage transforms through the software development kit — Transforms A P I, list Transforms V one in two point x — aligned with per-service paths. Still never hardcode opaque transform I Ds in runbooks as if they were portable across tenants; resolve by name.',
    'Say this: if firstValid plus lookup plus lower can express it, use a transform. Rules are the exception path. Workflows are for multi-step events with approvals or side effects — not for renaming attributes on aggregate.',
    'When transforms lose: multi-source orchestration with approvals; logic that needs complex loops or external calls beyond the transform vocabulary. Then you escalate to rule, workflow, or an external service — deliberately.',
    'Enterprise craft: naming conventions, sandbox-to-prod promotion, identity profile mapping review, and test identities with before-and-after attribute checks. Editing transforms only in prod is how silent identity corruption ships.',
    'Failure modes: BeanShell for string normalize; transforms that assume source display names differ across tenants with no documentation; dual-maintaining a transform and a rule for the same attribute.',
    'When you’re back at the screen: sketch a Normalize Email transform in words, answer transform-or-rule for lowercasing H R email, and note list Transforms V one as the two point x list call.',
  ],
  m13: [
    'Module thirteen is rules — BeanShell when transforms are not enough. Cold open: rules are privileged platform code. They are reviewed, installed through SailPoint process, and operationally heavier than JSON transforms. Use them sparingly.',
    'Outcomes: list rule use cases transforms cannot cover; explain review and install constraints at a conversational level; prefer connector customizers for SaaS source I O mutation when that path fits — you will deepen customizers in module sixteen.',
    'Boundary table in words: string, date, and lookup mapping — prefer transform. Complex branching plus state — rule or external plus A P I. Before and after SaaS connector operations — customizer in TypeScript. Approvals plus H T T P side effects — workflow.',
    'Rules earn their place for complex correlation, attribute logic beyond transform vocabulary, or legacy patterns your org already standardized on reviewed rules. They lose when a transform can say it, when you need event-driven multi-step process, or when you call arbitrary external A P Is on every aggregate without capacity planning.',
    'Ops notes: track which identity profiles and sources depend on each rule. Avoid silent dual-maintenance with a transform on the same attribute. For new SaaS sources, evaluate customizers before inventing a classic rule. Version rules, peer review them, and document blast radius.',
    'Say this in change review: default to transform. We open a rule only when the transform vocabulary cannot express the logic or platform constraints require it. We do not deploy rules like a casual git push — SailPoint process applies.',
    'Compared with customizers: for mutating SaaS connector payloads, TypeScript before and after hooks are often more flexible and maintainable than BeanShell. Do not reach for a classic rule out of habit when a customizer is the modern fit.',
    'Enterprise checklist voice: inventory of rules with owners; justification recorded when transform was insufficient; test plan on sandbox identities; previous version retained for rollback.',
    'Failure modes: rule sprawl for trivial string ops; undocumented dependencies that break aggregates after cleanup; assuming rules install like ordinary application code.',
    'When you’re back at the screen: pick one real attribute problem from your tenant, say transform or rule out loud with a one-sentence why, and if it is SaaS connector I O, note customizer as the alternative you must consider first.',
  ],
  m14: [
    'Module fourteen is workflows — event-driven automation and H T T P actions inside Identity Security Cloud. Cold open: workflows win for identity events that open tickets, notify channels, or chain ISC calls with approvals baked in. They lose for heavy bulk reconciliation — that is an external job plus software development kit.',
    'Outcomes: place workflows versus external S D K jobs versus transforms correctly; scan workflow H T T P actions for legacy yearly paths and slash latest; define error handling, secrets, and idempotency for those H T T P steps.',
    'Version debt hides here. Application code may already pin slash identities slash v one while a workflow H T T P action still posts to slash v two zero two five or slash latest. Legacy yearly paths lose support tickets through Q two twenty twenty-eight and hard stop in Q one twenty twenty-nine. Treat Workflow Analyzer mindset as mandatory in migration.',
    'H T T P action checklist spoken: pin per-service paths — no slash latest. Resolve object I Ds in earlier steps by name — including lifecycle state I Ds. Handle non-success statuses with explicit branches; do not swallow failures. Store secrets in the workflow secret store, never hardcoded in the action.',
    'Worked example: an identity lifecycle event triggers a workflow that resolves Terminated by name, then posts set lifecycle state on slash identities slash v one for that identity I D, then verifies or notifies on failure. Success on H T T P accept is not the same as every account already disabled — design for async provisioning lag.',
    'Say this: workflows are event-sized units of work. A bulk CSV compliance crunch belongs in an S D K job with dry-run. Attribute normalize on aggregate belongs in a transform. Do not stuff those jobs into workflows because the UI felt convenient.',
    'Loop hazard: a workflow mutates an identity attribute that re-triggers the same workflow. Add guards, idempotency keys, and clear ownership for failed executions. Someone must own the dead-letter pile.',
    'Enterprise craft: Workflow Analyzer in the migration pipeline; execution monitoring and alerting; documented trigger events and side effects; CI or review gates that flag yearly path prefixes in exported workflow definitions.',
    'Failure modes: yearly paths left in H T T P after app code migrated; infinite re-entry loops; no owner for failed runs; secrets pasted into action headers.',
    'When you’re back at the screen: open a sample workflow design, rewrite one imaginary H T T P URL from slash v two zero two six to a per-service pin, and answer whether bulk CSV compliance is workflow or S D K job.',
  ],
  m15: [
    'Module fifteen is SaaS Connectivity — custom TypeScript connectors. Cold open: if Identity Security Cloud must aggregate accounts and entitlements and provision as a Source, you need a connector. If you only call ISC when ITDR fires, you need an external integration — not a connector.',
    'Outcomes: explain when out-of-the-box is insufficient and SaaS Connectivity is justified; sketch command handlers for test connection, account list and read, entitlements, and provision; use spcx for local debug and know the loopback pattern’s risks.',
    'Package story: at-sailpoint slash connector-sdk plus spcx. This is not sailpoint-api-client. Confusing those two TypeScript surfaces is the module eight failure mode all over again. Connectors speak the Source contract; A P I clients speak platform REST.',
    'Command lifecycle order of proof: test connection first, then account list, then read and entitlement operations, then create, update, and disable. Prove aggregation before provisioning. A connector that can create but cannot list is not enterprise-ready.',
    'When to build: a SaaS app with no out-of-the-box connector, reachable from SailPoint cloud. Loopback — ISC managing ISC-shaped resources through Source semantics — is powerful and easy to get wrong; require an architecture decision record and rate-limit design.',
    'When not: on-prem-only systems that need virtual appliance or classic connectivity patterns; a simple nightly A P I job that does not need source aggregation semantics — that is module four, eight, or nine, not a connector.',
    'Say this: for a new SaaS H R app that should be an ISC Source, we use Connector S D K. For ITDR disable, we use REST or A P I S D K against lifecycle. Building a connector to open a ticket is usually a workflow H T T P action in disguise.',
    'Delivery craft: design account and entitlement schema before code; attach spcx test evidence to the pull request; map provisioning operations to ISC plan ops; name an owner team for connector version upgrades; rotate long-lived SaaS secrets in source config.',
    'Failure modes: connector for one-way ticket create that should be workflow; blocking aggregation on chatty unpaginated remote A P Is; no rotation plan for SaaS secrets; skipping list and read before Std Account Create.',
    'When you’re back at the screen: narrate the command order test, list, read, entitlements, provision — then answer A P I S D K versus Connector S D K for a new SaaS H R Source with no out-of-the-box option.',
  ],
  m16: [
    'Module sixteen is connector customizers — before and after hooks in TypeScript. Cold open: when an out-of-the-box or SaaS connector is almost right, mutate the I O — do not fork the whole connector, and do not reach for BeanShell by reflex.',
    'Outcomes: explain customizer versus full custom connector versus classic rule; identify before and after operation hooks as the extension point; plan testing so customizer bugs do not quietly corrupt provisioning.',
    'Weight ladder again: transform is lowest — identity attribute mapping on the profile. Customizer is medium — connector I O mutation. Full connector is highest — you own protocol and schema. Rule is heavy process — use when required, not because it is familiar.',
    'Customizers intercept SaaS connector operations. Typical jobs: sanitize attributes before account create; drop service accounts after account list; reshape fields the out-of-the-box mapping almost got right. Keep the logic pure, logged, and fixture-tested.',
    'Say this: customizer when an existing SaaS or out-of-the-box connector is nearly correct. Full connector when you own the protocol end to end. Transform when the need is identity-profile attribute mapping only. Rule when platform constraints or logic truly demand BeanShell.',
    'Why customizers often beat classic rules for SaaS sources: TypeScript before and after hooks on connector I O are usually more flexible and maintainable than BeanShell for those paths. Still treat them as production code — version with the source, never hot-fix only in prod.',
    'Testing bar: contract tests with sample input plan to expected mutated output. Sandbox proof before prod attach. Rollback procedure must include detach customizer. Silent entitlement drops in an after-list hook are career-limiting events.',
    'Enterprise checklist voice: customizer repo and continuous integration; fixture-based tests for before and after; source attachment documented; detach runbook rehearsed.',
    'Failure modes: customizer silently dropping entitlements; logic that belongs in transforms living in customizers “because TypeScript”; no sandbox proof; customizer used as a net-new protocol implementation instead of a real connector.',
    'When you’re back at the screen: pick customizer or full connector for a nearly-correct SaaS source that needs one field sanitized on create, write the one-sentence why, and list one fixture you would test before attach.',
  ],
}
