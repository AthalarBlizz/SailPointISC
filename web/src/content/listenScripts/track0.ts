/** Track 0 (Foundations) Listen scripts — coach voice, TTS-friendly. */

export const moduleListenScriptsTrack0: Record<string, string[]> = {
  m0: [
    'Cold open. Module zero is the platform model. If you confuse an identity with an account, you will pick the wrong A P I, break provisioning assumptions, and sound junior in a design review. Every Identity Security Cloud — I S C — integration mutates or reads a small set of objects. Learn the nouns first.',
    'Outcomes for this module. By the end you can draw the identity to account to entitlement to access profile to role chain from memory. You can explain where lifecycle state sits relative to provisioning and sources. And you can decide whether a change belongs in the U I, an A P I or S D K script, or a connector story.',
    'Teach — what I S C governs. An identity is the person or machine I S C governs. An account is that identity’s record on a specific source — Active Directory, Salesforce, a custom app. One identity usually has many accounts. Sources are the connected systems I S C aggregates from and provisions to.',
    'Teach — access composition. Entitlements are permissions or groups on a source. Access profiles bundle entitlements into reusable packages. Roles sit higher — business-shaped access that often groups access profiles. When someone says “give them finance access,” they almost always mean a role or profile, not a raw entitlement on one account.',
    'Teach — lifecycle and the identity profile. Lifecycle states like Active, Leave of Absence, or Terminated live on the identity profile for a population. Those states drive joiner, mover, and leaver provisioning downstream to managed sources. The identity profile also owns mapping and transforms. Lifecycle is the lever; accounts on sources are the effect.',
    'Teach — object resolution. Almost every I S C object is a tenant-specific GUID. Resolve by name at runtime — filter identities by alias, list lifecycle states on the profile and match Terminated by name, look up sources and roles by name. Never hardcode sandbox I Ds into a production script. Paths for new work look like slash identities slash v one, slash accounts slash v one, slash sources slash v one.',
    'Teach — who owns the change. H R and authoritative sources feed aggregation and transforms — that is batch truth on a schedule. Security and identity threat detection and response — I T D R — need A P I lifecycle or account disable in seconds. Business access goes through access request and approvals. A net-new SaaS app needs a connector story, not a nightly invent-accounts script.',
    'Example — say this in design review. Emergency disable sets the Terminated lifecycle state, resolved by name on the identity profile, so I S C cascades disables to managed sources. Peer clone submits access requests; it does not bypass approvals or assign entitlements directly. Aggregation lag is not a substitute for an A P I-driven leaver when security needs action now.',
    'Example — U I versus code. Configure in product when an admin can own the change and no external system must trigger it. Reach for the A P I or S D K when ServiceNow, a SIEM, or an automation must act, or when the U I cannot express the workflow. Extension points that are not “call the public A P I from a script” include transforms, rules, workflows, SaaS Connectivity connectors, and connector customizers.',
    'Failure modes. Treating account disable on one source as “identity offboarded” without a lifecycle change. Hardcoding lifecycle state or role GUIDs from sandbox into prod. Building per-app disable loops for managed sources that already provision from lifecycle. Confusing Search index lag with list-filter freshness when I T D R needs the authoritative identity right now.',
    'Failure modes, continued. Using access request to remove access for a compromised leaver — that is the wrong governance path. Inventing accounts overnight instead of onboarding a source. Assuming one entitlement equals one business role. Skipping before and after verification after any mutation.',
    'Screen close. Module zero is fluency of the object graph and ownership. Identity, account, entitlement, access profile, role, lifecycle, source — resolve by name, pick the right owner of change, and never confuse batch H R truth with security-speed A P I action. Next foundation module is authentication and scopes.',
  ],

  m1: [
    'Cold open. Module one is authentication, scopes, and least privilege. A failing call is diagnosable when you separate “who is this token” from “what is this token allowed to do.” Scripts and automations prefer a Personal Access Token — P A T — with the OAuth client credentials grant. Do not paste secrets into chat, tickets, or source control.',
    'Outcomes for this module. You can implement client-credentials token acquisition and reuse with about a twelve-minute time to live. You can separate the P A T owner’s user level from OAuth scopes when debugging a four oh three. And you can specify least-privilege scopes per integration instead of defaulting to scopes all.',
    'Teach — the token flow. Post grant type client credentials with the P A T client I D and client secret to the tenant OAuth token endpoint. Receive an access token. Send Authorization Bearer on every Identity Security Cloud — I S C — A P I call. Reuse one token for the run. Official S D Ks refresh automatically; raw REST must not mint a new token on every request.',
    'Teach — client I D shape and rate limits. Modern P A T client I Ds are UUIDs with dashes; legacy undashed I Ds fail auth in confusing ways. Think rate limits on the order of about one hundred requests per access token per ten seconds. Burning a new token per call is a common path to four twenty-nines and wasted latency.',
    'Teach — authentication versus authorization. A four oh one means the token is missing, expired, invalid, or pointed at the wrong base U R L. A four oh three means the token worked, but you are not allowed — missing scope, insufficient user level on the P A T owner, or an endpoint that needs a user context client credentials cannot supply. A four twenty-nine means back off with jitter and reuse the token you already have.',
    'Teach — scope posture by integration. Read-only reporting gets specific read scopes. An I T D R disable job needs identity lifecycle plus read, and you document blast radius. An access request bot gets request create and status — not admin bypass. A migration scanner is read-heavy with no mutate in dry-run. Name the P A T per integration and list scopes in the runbook.',
    'Example — say this when someone rotates the secret on a four oh three. Stop. Valid token with four oh three is almost never “bad secret.” Check scopes on the P A T, the owner’s user level, and whether the endpoint requires interactive user context. Rotating first and calling it done hides the real authorization gap.',
    'Example — operational hygiene. Store secrets in the OS keychain locally — this repo uses python-keyring — and a vault or K M S in production. Never commit a dot env with client secrets. Log correlation and request I Ds; never log the bearer token or client secret. Revoke the P A T when the integration identity is offboarded.',
    'Failure modes. New token on every request leading to four twenty-nines. Shared P A T across unrelated apps — audit and blast-radius nightmare. Assuming four oh three means bad secret. Using scopes all because “it worked in sandbox.” Leaving a personal engineer’s P A T in a production scheduler.',
    'Failure modes, continued. Wrong base U R L producing four oh ones that look like credential failures. Ignoring user level when scopes look correct. Skipping rate-limit headers and retry with backoff. Documenting nothing about which scopes a job actually needs.',
    'Screen close. Module one: P A T, one token per run, least privilege scopes, and a clean split between four oh one, four oh three, and four twenty-nine. Fail closed, name the credential, revoke on offboarding. Next up is the July two thousand twenty-six versioning dual world — the fluency upgrade leadership will ask you about.',
  ],

  m2: [
    'Cold open. Module two is the dual-world versioning brief for July two thousand twenty-six. This is the most important fluency upgrade in the foundations track. You must advise greenfield pins and brownfield migration with current facts — not blog posts from before the strategy update.',
    'Outcomes for this module. You can map legacy slash v two zero two six slash resource to slash resource slash v N using the official migration table. You can explain why slash latest is unsafe for production under the new strategy. And you can state support through Q two twenty twenty-eight and hard end of life in Q one twenty twenty-nine for legacy yearly, v three, and beta.',
    'Teach — why SailPoint moved. Yearly collections forced churn even when a service had no breaking change. Per-service semantic versioning versions each service independently. Major bumps happen only when the contract actually breaks. Paths look like slash identities slash v one, slash accounts slash v one, slash access-requests slash v one.',
    'Teach — the dual world. Legacy yearly paths still work — slash v two zero two six slash accounts, slash v three, beta, and slash latest. Greenfield work targets per-service slash resource slash v N. Prefer those for anything new. This workshop repo may still demonstrate yearly paths for patterns; treat that as teaching material, not a greenfield target.',
    'Teach — slash latest. Slash latest was introduced as a yearly-alias shortcut. Under the July two thousand twenty-six strategy, treat it as unsafe for production because routing can retarget silently when versions flip. Pin explicit service versions. Silent break on flip is worse than an obvious deprecation header.',
    'Teach — support and end of life. Support tickets for beta, v three, and yearly A P Is continue through Q two twenty twenty-eight. Endpoints stop functioning in Q one twenty twenty-nine. Migration is urgent strategically but not overnight — still, waiting until twenty twenty-nine with no inventory is a leadership failure mode.',
    'Teach — S D K two point x. TypeScript sailpoint-api-client two point x, Python sailpoint two point x, plus Go and PowerShell use resource-based A P Is with method version suffixes — list accounts v one, and so on. Old year namespaces go away. Run official migration scripts; do not hand-rewrite everything, then review v two outliers by hand.',
    'Example — say this in a design review. Greenfield pins slash service slash v N in REST and uses S D K two point x V one or V two methods. Brownfield inventories scripts, workflow H T T P actions, I T S M integrations, and R P A, then migrates before Q one twenty twenty-nine. Entitlements and some config paths may already be on v two — check the migration table; do not assume everything is v one.',
    'Example — workflows hide debt. Low-code workflow H T T P actions often still hit yearly paths. Use a Workflow Analyzer mindset even offline: find them, rewrite them, verify. Migrating S D K classes while leaving workflows on slash v two zero two five is unfinished migration.',
    'Failure modes. Shipping slash latest because “it always follows current.” Assuming every legacy path maps to v one. Treating end of life as a twenty twenty-nine problem with no inventory before support ends Q two twenty twenty-eight. Adding new yearly paths in C I templates after the strategy update.',
    'Failure modes, continued. Ignoring X-Deprecated headers in observability. Experimental endpoints in production without a gate. Quoting pre-migration advice from older workshop docs without correcting for per-service pins.',
    'Screen close. Module two: bilingual versioning. Prefer slash identities slash v one style pins, plan brownfield before Q one twenty twenty-nine, never trust slash latest in production, and migrate S D Ks with official scripts. Next foundation module is Open A P I as ground truth — never invent endpoints.',
  ],

  m3: [
    'Cold open. Module three is spec-driven development. During the dual-world period, model memory and old blog posts still invent slash v two zero two five paths. Open A P I specs are the only ground truth that survive the July two thousand twenty-six migration. Never invent endpoints.',
    'Outcomes for this module. You can locate an operation in the local api-specs identity now folder — current sailpoint-api dot yaml versus legacy yearly folders. You can extract method, path, scopes, and required headers before writing code. And you can explain why agentic coding rules must prefer specs over model memory while both worlds coexist.',
    'Teach — where truth lives. The current per-service collection lives in api-specs under identity now, sailpoint-api dot yaml. Legacy yearly folders like v two zero two five and v two zero two six remain for reading old code and workshop samples. Human docs help you navigate and find the migration table. S D K stubs come after you confirm the operation exists.',
    'Teach — the spec-first workflow. Find the operation in the highest-relevance current spec. Note the path shape slash service slash v N, required scopes, and any experimental flag. Only then choose an S D K method or write a REST call sheet. After any mutation, plan the verifying GET from the same spec. Path examples you should be able to say aloud include slash accounts slash v one and slash identities slash v one.',
    'Teach — agentic and C I discipline. Keep api-specs git-pulled. Point agents at the project rules file and the Y A M L. Reject pull requests that introduce yearly paths or slash latest without an explicit waiver. Prefer generated or typed clients over hand-rolled U R Ls. Ask for operation I D or spec path in the P R template.',
    'Teach — experimental and dual-world traps. Experimental operations may require special headers and should stay non-prod by default with an architecture decision record. Beta as a separate folder is legacy; new experiments appear in versioned A P Is with flags. Do not treat training-data endpoints as real.',
    'Example — say this before anyone writes list-accounts code. Open the current Open A P I first. Confirm path, query parameters, scopes, and response shape. Then pick S D K or REST. If the only match is under a legacy yearly folder, you are maintaining brownfield — say so explicitly — not targeting greenfield.',
    'Example — why keep legacy Y A M L after migrating. You still need to read and migrate existing scripts and workshop samples accurately until Q one twenty twenty-nine. Legacy specs are a translation aid, not a greenfield target. Current collection first; legacy only when the code in front of you is yearly.',
    'Failure modes. Copying slash v two zero two five paths from blog posts dated before the migration push. Trusting large language model endpoint memory during the dual-world transition. Generating S D K calls for operations that only exist under experimental headers. Shipping a hand-typed path that never appears in any checked-in spec.',
    'Failure modes, continued. Skipping scope extraction and discovering four oh three in production. No verifying GET planned from the same operation family. Updating docs to per-service while C I still scaffolds yearly templates.',
    'Screen close. Module three: Open A P I first, code second. Specs beat memory. Pull api-specs, extract path and scopes, then implement. Foundations track complete — next track starts with REST call sheets versus S D Ks for real integration craft.',
  ],
}
