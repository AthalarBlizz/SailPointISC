/** Track 2 Listen scripts — SDKs & CLI (m8–m11). Coach voice for Web Speech. */

export const moduleListenScriptsTrack2: Record<string, string[]> = {
  m8: [
    'Module eight is the TypeScript software development kit story for Identity Security Cloud. Cold open: if your team lives in Node, this is how you call the platform without hand-rolling every path — and without confusing three different TypeScript packages that all say SailPoint on the box.',
    'Outcomes for this lesson: scaffold Configuration with Accounts A P I and Identities A P I, call list methods with a V one suffix, tell A P I S D K apart from Connector S D K and customizers, and describe the one-point-x to two-point-oh migration rewrite.',
    'There are three TypeScript surfaces. First: sailpoint-api-client — the A P I S D K. It talks to ISC REST from Node or TypeScript services. Second: at-sailpoint slash connector-sdk plus spcx — that is SaaS Connectivity, for custom cloud connectors. Third: the customizer framework — TypeScript hooks that mutate before or after connector operations. Same language family, three different jobs.',
    'In two point x, you import resource A P Is without year namespaces. You get AccountsApi and IdentitiesApi, not Accounts V two zero two five Api. Method names carry the version: list Accounts V one, list Identities V one, and so on. That mirrors per-service paths like slash accounts slash v one.',
    'Configuration still reads SAIL base URL, SAIL client I D, and SAIL client secret from the environment. Prefer Personal Access Token auth for scripts. Reuse one token per run. Do not mint a fresh token on every call.',
    'CLI bootstrap is the team standard: sail sdk init typescript, then sail sdk init config. Pin the dependency major to two point x. When SailPoint ships a new service major, review the changelog — do not assume every method stays on V one forever.',
    'Say this in design review: a nightly compliance job uses the A P I S D K. A brand-new SaaS H R app with no out-of-the-box connector needs the Connector S D K. Do not put A P I client code inside a connector package and call it done.',
    'Worked example: resolve Jennifer by alias with list Identities V one and a filter, take the identity I D, then list Accounts V one filtered by that identity I D. Always resolve by name or alias at runtime — never hardcode tenant GUIDs from sandbox into prod.',
    'Migration: run the official migration script. It rewrites year-named A P Is to resource classes plus V one or V two suffixes. Review V two outliers by hand — entitlements and a few config endpoints are not blindly V one.',
    'Failure modes: importing Accounts V two zero two five Api in new code after a two point x upgrade; using the A P I S D K where connector-sdk is required; committing SAIL client secret in a dot env to make continuous integration “work.”',
    'When you’re back at the screen: open the TypeScript S D K lab, scaffold with the CLI, call list Identities V one once, and write one sentence naming which of the three surfaces your scenario needs.',
  ],
  m9: [
    'Module nine is Python for Identity Security Cloud automation. Cold open: this repo’s workshop samples may still show one point four with yearly namespaces. Production greenfield targets sailpoint two point x and per-service paths. You need to be literate in both — and honest about which one you ship.',
    'Outcomes: configure via environment or keyring and call version-suffixed methods; explain this workshop’s one point four pin versus the two point x production target; describe how migrate underscore sdk dot py moves year modules to resource A P Is.',
    'Auth pattern stays constant. Load credentials into the environment with the shared helper, then Configuration reads SAIL base URL, client I D, and client secret. Prefer Personal Access Token. Never hardcode secrets. Locally use the OS keychain; in production use a vault.',
    'Workshop literacy — one point four: imports look like sailpoint dot v two zero two five dot api, and methods often lack a version suffix. That still works on legacy yearly collections through the migration window. It is not the greenfield target after July two thousand twenty-six.',
    'Production shape — two point x: resource A P Is, methods like list underscore accounts underscore v one, aligned with slash accounts slash v one. Year and beta namespaces collapse. Experimental features still need the experimental header when the service requires it — the S D K can help, but you must know when it applies.',
    'After any lifecycle or account mutation, verify with a GET. Show before and after when you can. Dry-run flags belong on every mutator script that touches bulk identities. Mixing one point x and two point x packages in one virtual environment is a support nightmare — separate lockfiles.',
    'Say this to leadership: sailpoint dot v two zero two five is not broken in mid two thousand twenty-six — legacy yearly A P Is still function — but support tickets thin out by Q two twenty twenty-eight and hard end of life is Q one twenty twenty-nine. New jobs should land on two point x.',
    'Migration checklist: run the official Python migration tooling, re-test filters and pagination, confirm experimental headers, and open a ticket tied to those executive dates. Do not ship a new production job on yearly imports “because the workshop did.”',
    'When Python wins: ops automation, data jobs, notebook-adjacent tooling, and porting DevDays scenarios to production standards. When it loses: browser-only shops, or external ITSM teams who should receive a REST call sheet instead of a Python package.',
    'Failure modes: workshop imports in prod; mixed S D K majors in one venv; skipping verify GET after mutations; pasting client secrets into chat or source control.',
    'When you’re back at the screen: compare one workshop import path to the two point x shape in the module notes, then answer the drill — is v two zero two five broken, or is it a migration clock?',
  ],
  m10: [
    'Module ten is Go and PowerShell — same Identity Security Cloud versioning story, different operating homes. Cold open: after two thousand twenty-six, language choice is about skill coverage and runtime fit, not a different A P I worldview.',
    'Outcomes: know when Go or PowerShell is the right official software development kit versus raw REST; expect resource A P Is and versioned methods after two point x-style migrations; point teams at SailPoint’s language-specific migration scripts.',
    'What stays constant across TypeScript, Python, Go, and PowerShell: Personal Access Token auth, name-to-I D resolution, per-service version pins like slash identities slash v one, and operation or method version suffixes. Yearly collections are the thing you leave behind.',
    'PowerShell’s home is Windows admin runbooks, Entra and Active Directory ops bridges, and shops where PowerShell skill already outweighs Python packaging. Go’s home is platform services, compiled workers, and high-concurrency CLIs. Do not force Go when the org’s ISC muscle is Python.',
    'Fit test: if a gateway policy only allows outbound H T T P with a fixed call sheet, REST may be clearer than embedding an S D K. If operators live in PowerShell every day, official cmdlets beat a one-off curl library they will never maintain.',
    'Teach the migration expectation: stop year collections; call versioned service operations; run the Go or PowerShell migration script; then hand-review V two outliers. Blog posts from two thousand twenty-four will lie about cmdlet names — trust current docs after migration.',
    'Say this in an architecture decision record: we pick PowerShell because the operating model is Windows-centric runbooks, not because ISC is different in PowerShell. Auth still comes from vault-injected client I D and secret. Lifecycle states still resolve by name before set.',
    'Worked sketch: connect with vault-backed credentials, search or get identity by alias equals j dot doe, resolve the Terminated lifecycle state by name on the identity profile, then set lifecycle and verify. Same discipline as every other language.',
    'Cross-language footgun: three services in three languages with no shared A D R on versioning. You get silent yearly paths in one repo and per-service pins in another. Pick a house standard and lint for banned prefixes.',
    'Failure modes: assuming old cmdlet names still match; embedding secrets as clear-text script parameters on jump hosts; treating Go as mandatory prestige when the on-call team cannot read it.',
    'When you’re back at the screen: write two bullets — when PowerShell is the default, when Go is — then note which migration script your chosen language must run before Q one twenty twenty-nine.',
  ],
  m11: [
    'Module eleven is the SailPoint command line interface. Cold open: the CLI is how teams bootstrap the same project shape — it is not how production authenticates at three in the morning.',
    'Outcomes: init TypeScript or other S D K projects and config via CLI; explain the CLI’s role beside spcx in connector local debug; keep CLI config free of committed secrets.',
    'Core habit: sail sdk init typescript with a project name, then sail sdk init config. Commit the template layout, not every engineer’s personal folder taxonomy. Document the required CLI version in the README so onboarding does not drift.',
    'Separate scaffolds: an A P I project that depends on sailpoint-api-client is not a connector project that depends on connector-sdk. Mixing those packages casually creates confused ownership and wrong deployment paths.',
    'Connector workflows use the CLI plus spcx for local debug loops — you will deepen that in module fifteen. For now: CLI scaffolds and wires local config; spcx exercises connector commands against a running target.',
    'Config hygiene: treat CLI config like a dot env file — local only, gitignored. Prefer keyring or environment injection over long-lived plaintext profiles. Continuous integration must inject vault secrets non-interactively. Developer workstation login patterns do not belong in headless runners.',
    'Say this to a new hire: we scaffold with sail sdk init so every service looks alike. We never commit sail config with client secrets. Production auth is vault-managed, rotated, audited Personal Access Tokens — not your laptop CLI profile.',
    'Team standards: pin CLI version; distinguish A P I versus connector templates; ban interactive auth in pipelines; review pull requests for accidental config commits the same way you review for secrets.',
    'When the CLI is the wrong tool: high-volume production integrations should run as services with S D Ks and observability, not as someone re-running CLI commands. The CLI bootstraps; the service operates.',
    'Failure modes: committing secrets in sail config; every engineer hand-rolling different TypeScript layouts; using interactive CLI login in headless prod runners; treating CLI as a substitute for secret management.',
    'When you’re back at the screen: run or narrate sail sdk init typescript and sail sdk init config, confirm gitignore covers local config, and answer why the CLI is not enough for production auth.',
  ],
}
