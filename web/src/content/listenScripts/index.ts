/**
 * Listen scripts are authored in docs/listen-scripts/ (Markdown).
 * This module loads them for the app — do not duplicate script text here.
 */
export {
  getListenScript,
  phaseListenScripts,
  moduleListenScripts,
} from './loadFromMarkdown'
